name: master

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  api-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: API Tests
        run: |
          cd src/api
          npm run build
          docker-compose up -d > /dev/null
          sleep 50
          docker exec juanportal_api npm run test
#      - name: API Format Check
#        run: |
##          npm run fmt:check

  client-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Client Tests
        run: |
          docker-compose up -d > /dev/null
          sleep 200
          docker exec juanportal_node npm run test
          docker exec juanportal_node npm run testReact

  socket-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Socket Tests
        run: |
          docker-compose up -d > /dev/null
          sleep 60
          docker exec juanportal_socket npm run testTS