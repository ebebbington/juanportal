extends layout
block append head
block append sidebar
    a.add-profile-link.sidebar-item(href="/profile/add") Add a Profile
block append content
    #button-container
    #profile-container
   
    script(data-plugins="transform-es2015-modules-umd" type="text/babel" src="/public/javascripts/components/Profile.js")
        //- // When no profiles exist
        //- if (!people.length)
        //-     .well.no-profiles
        //-         h3 Oh no, there aren't any profiles!
        //-         a.add-profile(href="/profile/add") Why not be the first and add a new profile?
        //- // When profiles do exist
        //- each person in people
        //-     .well
        //-         img(src=`${person.image}`)
        //-         h1 #{person.name}

    script(data-plugins="transform-es2015-modules-umd" type="text/babel").
        import Profile from '../public/javascripts/components/Profile.js'
        ReactDOM.render(
            <Profile count="5" />,
            document.getElementById('profile-container')
        )

            