extends layout
block append header
    link(rel="stylesheet", href="/public/stylesheets/index.css")
block content
    // Main content
    .container
        // Side bar list
        .col-xs-3.col-sm-3.col-md-3
            #sidebar
                a#add.sidebar-items(href="/profile/add") Add a Profile
                a#home.sidebar-items(href="/") Home
        // Brief profiles
        .col-xs-9.col-sm-9.col-md-9.people
            if (!people.length)
                .p No profiles were found
            each person in people
                .person.well
                    .col-xs-4.col-sm-4.col-md-4
                        img.image(alt="Image of user", src=`${person.image}`)
                        a.view-profile.profile-actions(href=`/profile?id=${person._id}`) View Profile
                        a.delete-profile.profile-actions(href=`/profile/delete?id=${person._id}`) Delete Profile
                    .col-xs-8.col-sm-8.col-sm-8
                        h2.person-name #{person.name}
                br