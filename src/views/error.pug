extends layout
block content
    div.container
        // if in the 500 range
        if (/^5\d{2}$/).test(title)
            p Something went wrong!
        // if in the 400 range
        if (/^4\d{2}$/).test(title)
            p Not found